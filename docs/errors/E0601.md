# E0601: unsupported injection language

The `@inject->LANGUAGE` block names a foreign-language backend that this build of
PunPun does not support. PunPun 0.5 currently implements cached native C
injection. Other injection adapters remain staged work.
