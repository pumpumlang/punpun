# E0801: raw pointer dereference requires unsafe

Raw pointer dereference is a native-memory operation and must be inside an
`unsafe { ... }` block. Safe references (`&T` and `&mut T`) do not require an
unsafe block for ordinary dereference.
