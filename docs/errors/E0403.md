# E0403: contract conformance failure

A type declared with `meets ContractName` is missing a required method or its
signature does not match the contract.

Contract conformance is compile-time metadata in the current 0.5 beta. It does
not force heap allocation or virtual dispatch.
