# E0401: unknown object/struct member

A field or method name does not exist on the statically known receiver type.
PunPun checks visible members and may suggest the closest valid spelling.

```punpun
player.attak(); // if `attack()` exists, PunPun suggests it
```
