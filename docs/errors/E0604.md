# E0604: invalid injection interface

An `extern native fn` declaration uses a type or calling shape that the current
native ABI bridge cannot represent safely. The 0.5 beta direct x86-64 bridge is
intentionally conservative while C ABI support grows.
