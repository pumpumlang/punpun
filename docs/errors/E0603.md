# E0603: injected compilation failed

The compiler for an explicitly embedded foreign-language block returned a
failure. PunPun prints the foreign compiler diagnostics and then reports E0603
at the injection site.

`pp check` never compiles or executes injected source. Foreign compilation only
happens during build/run, and unchanged injected C is reused from the content
cache.
