# E1716 — match is not exhaustive

Every algebraic enum variant and both boolean values must be covered. Integer
and string domains are open-ended and therefore require a wildcard arm. Add the
listed missing variants or finish the match with `_ => expression`.
