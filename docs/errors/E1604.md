# E1604 — generic constraint is not satisfied

A concrete type selected for a generic parameter does not implement every
required contract, or it is not eligible for compiler-known `Copy` behavior.
Use a satisfying type, implement the named contract, or remove a constraint
that the generic body does not require. Identity objects are not `Copy`.
