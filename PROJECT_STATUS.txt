PunPun 0.6 development snapshot — Step 1

Current milestone:
- canonical repository version source and generated consistency checks
- frozen 0.6 type, generic, enum, nullability, ownership and compatibility specifications
- generic declaration/type parser scaffolding for tooling
- explicit E0900 gates for reserved enum, match and propagation syntax
- 0.5 compatibility fixtures retained

Expected local release verification:
- clean C17/C++17 warnings-as-errors build
- full compiler/runtime/tooling suite
- direct Linux x86-64 and portable C regression coverage
- self-hosted stage-one/stage-two fixed point
- documentation and PPX static-site builds
- isolated Linux installer build/run/uninstall validation
- source privacy and artifact checksum audit

Platform truthfulness:
- Linux x86-64 artifacts are built and executable on the release host.
- Arch package structure is generated locally; makepkg/pacman qualification requires the Arch CI job or a real Arch/CachyOS host.
- Windows WiX sources are packaged locally; MSI/setup binaries and install/upgrade/uninstall claims require the Windows CI job.

Next milestone:
- Step 2 implements generic calls, inference, constraints, monomorphization and specialization caching.
