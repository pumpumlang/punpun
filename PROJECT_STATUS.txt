PunPun 0.7.0-dev.1 — Step 7 Phase 7.1 complete; Machine IR/ABI foundation active

Completed foundation from 0.6:
- deterministic generics/monomorphization and constraints
- algebraic enums, Option/Result, exhaustive match and postfix ?
- control-flow move-state analysis, borrow checking and checked Slice<int> views
- mandatory verified typed HIR -> MIR pipeline with stable fingerprints
- direct Linux x86-64 backend, portable C backend and optional Clang/LLVM path
- compiler-backed LSP/editor, PPX, self-host bootstrap seed, docs and packaging
- deterministic PP branding, source/release hygiene and qualify-before-promote publisher

Step 7 Phase 7.1 implemented in 0.7.0-dev.1:
- target-aware Machine IR below verified MIR
- explicit PunPun argument-block ABI and SysV AMD64 native ABI metadata
- record hidden-result-pointer layout and byte-accurate argument offsets
- call-barrier-aware liveness
- stronger linear-scan physical allocation with callee-saved call-live placement
- farthest-end register eviction and verified stack spill slots
- Machine IR verifier for block validity, definition/use order, allocation overlap,
  call-clobber safety and 16-byte frame alignment
- `ppc emit-machine-ir` inspection command
- direct x86 backend consumes Machine IR function authority and ABI layouts
- portable C backend consumes Machine IR function authority
- function body fingerprints include Machine IR ABI/allocation identity

Known release qualification issue carried from beta.1:
- the beta.1 Arch Actions job failed before makepkg/pacman qualification completed;
  the release publisher correctly blocks promotion on this failure
- Linux fast CI was green in the observed beta.1 run; Windows/Arch qualification
  remain separate release gates and must not be claimed green without successful runs
- 0.7 source development proceeds independently from that blocked 0.6 prerelease promotion

Next Step 7 work:
- Phase 7.2: remove remaining typed-AST detail dependencies from direct x86 body lowering
- Phase 7.3: function/module object-level incremental reuse and dependency invalidation
- Phase 7.4: Machine IR optimization, spill/reload placement and allocation maturity
- Phase 7.5: diagnostics/fix-its, module/API/FFI hardening and tooling quality gates
